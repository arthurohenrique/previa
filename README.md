# Prévia

Next.js 16.3. Sistema em reconstrução — estrutura anterior removida.

```bash
pnpm install
pnpm dev
```

## Banco

`teardown.sql` derruba o schema Supabase da versão anterior. Rode uma vez no
SQL Editor e apague o arquivo depois.
